import { Discovery } from "@/components/discovery";import { ProjectShell } from "@/components/project-shell";import { getDiscoveryState } from "@/lib/discovery-repository";import { getAuthorizedProject } from "@/lib/page-data";
export default async function DiscoveryPage({params}:{params:Promise<{id:string}>}){const{id}=await params,{context}=await getAuthorizedProject(id),state=await getDiscoveryState(context.projectId);return <ProjectShell id={id} active="discovery"><main className="page"><div className="eyebrow">Understand before generating</div><h1>Adaptive discovery</h1><p className="subtle">Answers persist; skips remain unknown and cannot inflate confidence.</p><Discovery projectKey={id} initial={state} canEdit={context.role!=="VIEWER"}/></main></ProjectShell>}
