import { ArtifactListPage } from "@/components/artifact-list-page";import { getAuthorizedProject } from "@/lib/page-data";export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params,{project}=await getAuthorizedProject(id);return <ArtifactListPage project={project} active="requirements" title="Requirements" description="Stable, testable statements of product behavior and constraints." types={["requirement","business-rule"]}/>}
