import { ArtifactWorkspace } from "@/components/artifact-workspace";import { getAuthorizedProject } from "@/lib/page-data";import { listArtifactVersions } from "@/lib/project-repository";
export default async function Blueprint({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<{artifact?:string}>}){const{id}=await params,{artifact}=await searchParams,{context,project}=await getAuthorizedProject(id),selected=project.artifacts.find(a=>a.id===artifact)??project.artifacts[0],versions=await listArtifactVersions(context.projectId,selected.id);return <ArtifactWorkspace project={project} initial={selected} initialVersions={versions} canEdit={context.role!=="VIEWER"}/>}
