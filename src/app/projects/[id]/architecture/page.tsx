import { ArtifactListPage } from "@/components/artifact-list-page";import { getAuthorizedProject } from "@/lib/page-data";export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params,{project}=await getAuthorizedProject(id);return <ArtifactListPage project={project} active="architecture" title="Architecture" description="System boundaries, runtime components, security, and deployment strategy." types={["architecture","api","security"]}/>}
