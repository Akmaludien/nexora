import { GraphView } from "@/components/graph-view";import { ProjectShell } from "@/components/project-shell";import { getAuthorizedProject } from "@/lib/page-data";
export default async function GraphPage({params}:{params:Promise<{id:string}>}){const{id}=await params,{project}=await getAuthorizedProject(id);return <ProjectShell id={id} active="knowledge-graph"><main className="page"><div className="eyebrow">Project knowledge</div><h1>Knowledge graph</h1><p className="subtle">Select a node to inspect persisted incoming and outgoing relationships.</p><GraphView project={project}/></main></ProjectShell>}
