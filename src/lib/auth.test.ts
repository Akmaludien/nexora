import { describe,expect,it } from "vitest";import { constantTimeEqual,hasSameOrigin } from "./auth";
describe("authentication boundaries",()=>{it("compares sensitive values in constant-time shape",()=>{expect(constantTimeEqual("same","same")).toBe(true);expect(constantTimeEqual("same","diff")).toBe(false);expect(constantTimeEqual("short","longer")).toBe(false)});it("enforces same-origin mutations",()=>{expect(hasSameOrigin(new Request("http://local/api",{headers:{origin:"http://local",host:"local"}}))).toBe(true);expect(hasSameOrigin(new Request("http://local/api",{headers:{origin:"https://attacker.example",host:"local"}}))).toBe(false)})});
